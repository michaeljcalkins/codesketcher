import React from 'react'

export default function () {
  const styles = {
    lineHeight: '47px',
    textAlign: 'center',
    width: '5%',
    height: '47px',
    float: 'left'
  }

  return (
    <div style={styles}>
      <i className='fa fa-forward' />
    </div>
  )
}
